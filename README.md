# JS-Applications-Demos

//In the current state of the files, they can be run with the help of the server, which has also been added in the respository,
//The packages to be installed are the following :{npm i lit-html page lite-server/live-server(if localhost:{any} has been set}
